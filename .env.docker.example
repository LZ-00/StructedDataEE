# Docker 端口映射（宿主机）
# 默认保持 3000/8000，便于与开发模式一致。
FRONTEND_PORT=3000
BACKEND_PORT=8000

# 绑定地址（可选）
# 0.0.0.0: 允许局域网访问；127.0.0.1: 仅本机访问。
FRONTEND_BIND_HOST=0.0.0.0
BACKEND_BIND_HOST=0.0.0.0

# 后端基础鉴权（可按需覆盖）
SECRET_KEY=sdweb-dev-secret-change-in-production
DEFAULT_USERNAME=root
DEFAULT_PASSWORD=123456
